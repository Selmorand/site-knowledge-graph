[phases.setup]
nixPkgs = ['nodejs_20', 'python3']

[phases.install]
cmds = [
  'npm ci',
  'npx playwright install-deps chromium',
  'npx playwright install chromium'
]

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm start'
